@page
@model SuccessModel
@{ ViewData["Title"] = "Submitted"; }

<div class="success-wrap">
    <div class="success-card">
        <div class="success-icon">&#10003;</div>
        <div class="success-title">@(Model.IsAudit ? "Head of Department / Shift Manager audit submitted!" : "Shift submitted!")</div>
        <div class="success-sub">Saved successfully.</div>
        <div class="success-ref">Ref #@Model.SubmissionId</div>
        @if (Model.IsAudit)
        {
            <a asp-page="/AuditResult" asp-route-id="@Model.SubmissionId" class="welcome-btn primary">View result</a>
            <a asp-page="/AuditStart" class="welcome-btn secondary">New audit</a>
            <a asp-page="/AuditHistory" class="welcome-btn secondary">Audit history</a>
        }
        else
        {
            <a asp-page="/Index" class="welcome-btn primary">Back to home</a>
        }
        <a href="/api/submissions/@Model.SubmissionId/pdf" class="welcome-btn secondary">&#8595; Download PDF</a>
    </div>
</div>
